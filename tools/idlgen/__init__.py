# UnDOS IDL compiler package.
#
# This package was split out of the original single-file
# tools/create-syscalls.py script for maintainability. See cli.py for the
# command-line entry point (still invoked as `create-syscalls.py`).
